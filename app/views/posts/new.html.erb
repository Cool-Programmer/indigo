<%= form_for :post, url: posts_path do |f| %>

  <p>
    <%= f.label :title, "Title" %> <br>
    <%= f.text_field :title %>
  </p>

  <p>
    <%= f.label :body, "Body" %> <br>
    <%= f.text_area :body %>
  </p>

  <%= f.submit "Add Post", :disable_with => 'Adding...' %>

<% end %>
